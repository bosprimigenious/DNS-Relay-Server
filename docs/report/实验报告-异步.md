# DNS 中继服务器 — 实验报告（异步中继版 · 分支 relay-async）

| 项目 | 内容 |
|------|------|
| **课题名称** | DNS 中继服务器（DNS Relay Server） |
| **小组成员** | 张恒基（2024210926）、尹浩铭（2024210910）、林旭东（2024210915） |
| **班级** | 2024211301 |
| **完成日期** | 2026 年 6 月 |

---

## 课程要求与交付规范（对照参考资料 / docs）

本报告撰写依据项目内以下文档与资料（与 BUPT 计算机网络课程设计 DNS 中继课题一致）：

| 来源 | 路径 | 与本报告的关系 |
|------|------|----------------|
| 课程设计说明 | `参考资料/计算机网络课程设计-DNS(6).pptx`（课题 PPT） | 三大功能：本地拦截、本地解析、上游中继；UDP 53；配置文件 `dnsrelay.txt` |
| 协议规范 | `参考资料/RFC1035.TXT` / `RFC1035.pdf` | 报文 12 字节首部、域名编码、RCODE、指针压缩 |
| 配置文件样例 | `参考资料/dnsrelay.txt` | 208 条 `IP 域名` 记录；`0.0.0.0` 表示拦截 |
| 编码任务清单 | `docs/dev/TODO_MCP.md` Phase 1–7 | 模块划分、编译零警告、`make` 验收、报告截图与 PDF 交付 |
| 使用与测试 | `README.md`、`docs/test-output.txt` | 运行方式、`dig`/`nslookup` 用例、集成测试输出 |
| 实测日志 | `docs/verification/` | 终端实录原文；**未自动化成功的用例需组员自行补测并截图** |

**课程功能验收要点（PPT + `README.md`）：**

1. **本地拦截**：配置 IP 为 `0.0.0.0` → 返回 NXDOMAIN（RCODE=3）。
2. **本地解析**：配置真实 IPv4 → 对 QTYPE=A 返回 A 记录。
3. **上游中继**：未配置域名 → 转发至 `114.114.114.114`，还原 Transaction ID 后回客户端。
4. **工程要求**：`select()` 事件驱动主循环；模块化 C 源码；WSL/Linux 编译运行。

**交付清单（`docs/dev/TODO_MCP.md` §7.3 / `README.md`）：**

`实验报告.pdf`、`README.md`、`include/`、`src/`、`Makefile`、`参考资料/dnsrelay.txt`、`.gitignore`。

**测试与截图要求（`docs/dev/TODO_MCP.md` §7.1b）：**

至少包含：`nslookup bupt`（本地解析）、`nslookup 008.cn`（拦截）、`nslookup baidu.com`（中继）。本组已用 `dig` 补充 fix-A / fix-B 用例；**fix-B（上游超时 SERVFAIL）需组员本机执行 `iptables` 后自行截图**（见第四章 §4.4.5 与 `docs/verification/04-fixB-servfail-note.txt`）。

---

## 摘要

本报告专述 **异步上游中继**（Git 分支 `relay-async`）：`select()` 同时监听 client/upstream 两 socket；`handle_upstream_response` + `find_record_by_new_id` 完成回包路由。同步版见 `main` → `docs/report/实验报告-同步.md`。

实现过程中，我们重点处理了 RFC 1035 报文格式、网络字节序转换、域名指针压缩、Transaction ID 映射、`select()` 事件驱动主循环等关键问题。测试表明，系统在本地解析、域名拦截、公网中继及 QTYPE 边界场景下均能给出符合协议预期的响应。本报告从需求背景、系统设计、实现思路、测试分析与总结反思五个层面，对项目全过程进行说明。

**关键词**：DNS · UDP · RFC 1035 · 中继服务器 · select · 网络字节序

---

## 第一章 需求分析

### 1.1 背景与课题意义

#### DNS 在互联网中的角色

当用户在浏览器地址栏输入 `www.bupt.edu.cn` 并按下回车时，计算机并不会直接连接这个字符串。操作系统会先把域名交给 DNS 解析服务，询问「这个域名对应的 IPv4 地址是什么？」；得到答案（例如 `123.127.134.10`）后，才会向该 IP 发起 HTTP/TCP 连接。

因此，DNS 处于应用层与传输层之间的关键位置：它负责把**人类可读的域名**映射为**机器可用的 IP 地址**。没有 DNS，互联网的大规模部署几乎不可能实现。

#### 本课题要做什么

本课程设计不要求我们实现一个功能完整的递归 DNS 服务器（不必维护根区、顶级域区、完整缓存体系等），而是实现一个**轻量级 DNS 中继（Relay）服务器**：

1. 在本机 **UDP 53 端口** 监听客户端 DNS 查询；
2. 查阅本地策略表，决定对该域名**拦截**、**本地应答**还是**转发上游**；
3. 构造或转发 DNS 响应，返回给发起查询的客户端。

这种「中继 + 本地策略」模型，与校园网、企业网中常见的 DNS 过滤、内网域名映射场景十分接近，具有明确的工程背景。

#### 与生产级 DNS 的差异（刻意简化）

| 能力 | 生产级递归 DNS | 本课题实现 |
|------|----------------|------------|
| 域名数据库 | 完整递归查询链 | 仅本地 txt 配置表 |
| 缓存 | TTL 感知缓存 | 无 |
| 传输协议 | UDP + TCP（大包） | 仅 UDP |
| 记录类型 | A/AAAA/MX/CNAME 等 | 主要处理 A 记录 |
| 并发模型 | 多进程/异步 I/O | 单线程 + select |

我们有意聚焦 RFC 1035 报文、UDP Socket、`select()` 多路复用和本地策略表，把课程知识点落到一份可编译、可演示、可测试的完整程序上。

### 1.2 一次 DNS 查询在本系统中的生命周期

理解「一条查询从进入到离开」的全过程，有助于把握整体设计：

1. **客户端**（浏览器、`nslookup` 等）向本机 53 端口发送 UDP 查询报文，报文内含域名、QTYPE（查询类型）和 Transaction ID；
2. **主循环**通过 `select()` 感知 socket 可读，调用 `recvfrom` 读入报文；
3. **协议模块**解析 Question 段，提取域名与 QTYPE；若报文非法，返回 FORMERR；
4. **配置模块**在 `dnsrelay.txt` 中查找域名：
   - 命中 `0.0.0.0` → 构造 NXDOMAIN 响应（拦截）；
   - 命中真实 IP 且 QTYPE=A → 构造 A 记录响应（本地解析）；
   - 命中真实 IP 但 QTYPE≠A → 构造空 NOERROR 响应；
   - 未命中 → 进入上游中继；
5. **中继模块**替换 Transaction ID，转发至 `114.114.114.114`，等待响应后还原 ID 并回传客户端；
6. 客户端收到响应，完成一次解析。

> **设计要点**：每一步都有明确的「失败路径」——格式错误、上游超时、表满等场景均不会 silent failure，而是返回对应 RCODE，保证客户端行为可预期。

### 1.3 功能需求

| 模式 | 触发条件 | 对外行为 | 典型场景 |
|------|----------|----------|----------|
| **本地拦截** | 配置 IP 为 `0.0.0.0` | RCODE=3（NXDOMAIN） | 屏蔽广告/恶意域名 |
| **本地解析** | 配置非零 IPv4 | 返回 A 记录（TTL=300s） | 内网域名、课程测试域 |
| **上游中继** | 域名不在配置表 | 转发上游，还原 ID 后返回 | 访问 baidu.com 等公网域 |

配置文件：`参考资料/dnsrelay.txt`，每行 `IP 域名`，支持 `#` 注释。路径在 `main.c` 中**硬编码为相对路径**，因此必须在项目根目录执行 `./dnsrelay`；加载失败则打印 warning 并退化为纯中继模式（本地拦截/解析失效）。

### 1.4 非功能需求

- **协议合规**：报文格式、字节序、QTYPE 一致性符合 RFC 1035；
- **CPU 友好**：主循环用 `select()` 带 10ms 超时，避免忙等待；
- **模块化**：协议、配置、ID 映射、主控分文件实现；
- **可测试性**：支持 `nslookup`/`dig`/自研脚本分别验证三种模式。

### 1.5 DNS 协议要点（与本项目相关）

DNS 报文经 **UDP** 传输，单条上限 **512 字节**，结构为：**12 字节首部** + Question/Answer/Authority/Additional 区段。

**首部关键字段：**

| 字段 | 宽度 | 作用 |
|------|------|------|
| ID | 16 bit | 匹配请求与响应；中继时必须正确还原 |
| FLAGS | 16 bit | QR（查询/响应）、RCODE（结果码）等 |
| QDCOUNT | 16 bit | Question 段记录数（本系统要求 =1） |
| ANCOUNT | 16 bit | Answer 段记录数 |

**域名编码**：长度前缀标签，如 `bupt` → `\x04bupt\x00`；应答可用 **指针压缩**（高 2 位 `11`）引用 Question 中已出现的域名。

**本系统使用的 RCODE：**

| 值 | 名称 | 含义 |
|----|------|------|
| 0 | NOERROR | 成功；非 A 本地命中时为空应答 |
| 1 | FORMERR | 报文无法解析 |
| 2 | SERVFAIL | 上游超时或转发失败 |
| 3 | NXDOMAIN | 本地拦截 |

---

## 第二章 系统设计

### 2.1 总体架构

采用「**单进程 + 单监听 Socket + 事件驱动主循环**」：

```
  客户端 ──UDP:53──► 主控（select 循环）
                         │
           ┌─────────────┼─────────────┐
           ▼             ▼             ▼
        协议模块      配置模块      中继模块
      解析/构造响应   dnsrelay.txt   114.114.114.114
```

**选型理由：**

- **单进程单线程**：DNS 查询报文短、逻辑简单，课程并发量下单线程足够；避免多线程带来的 ID 表竞态；
- **事件驱动**：`select()` 在无流量时不占满 CPU，有流量时及时响应；
- **模块分离**：协议细节与业务调度解耦，便于单独调试与替换（如将来升级为 epoll）。

### 2.2 模块划分

| 模块 | 文件 | 职责 |
|------|------|------|
| 协议层 | `dns_protocol.*` | 报文解析、域名编解码、各类响应构造 |
| 配置层 | `config.*` | 加载/查询本地域名—IP 表 |
| ID 映射层 | `id_map.*` | 记录中继时的 ID 与客户端地址，超时清理 |
| 主控层 | `main.c` | Socket 生命周期、select 循环、三路分支调度 |

模块间仅通过头文件声明的接口交互，主控层不直接操作报文字节偏移。

### 2.3 主循环与事件驱动

**问题**：若主循环在无数据时不断 `recvfrom`，会 busy loop，CPU 占用接近 100%。

**方案**：`select(sockfd, timeout=10ms)` —— 有数据立即处理；无数据最多等 10ms 再进入下一轮，兼顾响应延迟与 CPU 占用。

**分支逻辑**（文字描述）：

1. 报文长度 < 12 → 静默丢弃（可能是扫描流量）；
2. 解析失败 → FORMERR；
3. 配置命中 `0.0.0.0` → NXDOMAIN；
4. 配置命中非零 IP：QTYPE=A → 本地 A 记录；否则 → 空 NOERROR；
5. 未命中 → 上游中继；失败/超时 → SERVFAIL。

### 2.4 三种业务分支的设计思路

#### （1）本地拦截

配置 `0.0.0.0 域名` 表示「该域名不应被解析」。不转发上游，直接返回 NXDOMAIN。客户端（浏览器、`nslookup`）会理解为「域名不存在」，达到屏蔽效果。

#### （2）本地解析

配置真实 IP 时，本机扮演「该域名的应答者」。复制原查询的 Header + Question，在 Answer 段填入 A 记录，NAME 字段用指针压缩引用 Question 中的域名。

**重要边界**：仅 QTYPE=A 时返回 A 记录。对 MX/AAAA 等查询，即使域名在表中，也应返回空 NOERROR，否则 TYPE 与 QTYPE 不一致，部分客户端会异常。

#### （3）上游中继

未配置域名需转发公网 DNS。**Transaction ID 冲突**是中继的核心难点：多客户端可能使用相同 ID，转发时必须分配新 ID 并记录映射，收到上游响应后再还原 ID 回传。

当前为**同步中继**（发完等响应，超时 3 秒），处理一条中继时主循环暂时无法收新包；对课程规模可接受，高并发需改异步。

### 2.5 ID 映射表

固定容量 1024 条，环形槽位分配 + 线性探测找空位。每条记录含：原始 ID、新 ID、客户端 IP/端口、创建时间。

- 每次收包清理超过 **5 秒** 的过期记录；
- 表满时强制清空全部记录后重试一次，避免偶发高峰导致中继彻底失败。

### 2.6 小组分工（按模块 + 按章节）

本组三人按「**代码模块负责人**」与「**报告章节负责人**」双线分工，联调与测试阶段全员参与。

#### 2.6.1 代码实现分工

| 成员 | 学号 | 负责文件 / 模块 | 具体任务（可验收） |
|------|------|-----------------|-------------------|
| **张恒基** | 2024210926 | `src/main.c` | ① `socket/bind` 与 `DNS_RELAY_BIND` / `DNS_RELAY_PORT` 环境变量；② `select()` 10ms 主循环；③ 三路分支调度（拦截 / 本地 / 中继）；④ `relay_to_upstream()` 同步转发、3s `SO_RCVTIMEO`、失败回 `SERVFAIL`；⑤ fix-B 返回值检查 |
| **尹浩铭** | 2024210910 | `include/dns_protocol.h`、`src/dns_protocol.c` | ① DNS 首部与位域、主机序/网络序转换；② `dns_name_decode/encode`（指针压缩、防死循环）；③ `dns_parse_query`；④ `dns_build_a_response` / `dns_build_error_response`；⑤ fix-A 相关响应字段（`ancount=0` 空 NOERROR） |
| **林旭东** | 2024210915 | `config.*`、`id_map.*`、`scripts/` | ① `参考资料/dnsrelay.txt` 加载与 `strcasecmp` 查表；② ID 映射表插入与 5s 超时清理；③ `dns_query.py`、`test_dns.sh`、`run_verification.sh`；④ 对照 `dig` 整理测试日志与报告第四章 |

#### 2.6.2 实验报告撰写分工

| 章节 | 主笔 | 协作 |
|------|------|------|
| 第一章 需求分析 | 张恒基 | 尹浩铭补充 RFC 1035 字段说明 |
| 第二章 系统设计 | 尹浩铭 | 张恒基补充主循环与中继流程图 |
| 第三章 关键实现 | 尹浩铭（协议）+ 张恒基（中继） | 林旭东补充配置与 ID 表 |
| **第四章 测试与结果** | **林旭东** | 三人共同执行终端验证并截图 |
| 第五章 总结 | 张恒基 | 全员补充问题与收获 |

#### 2.6.3 联调与 Code Review 修复

| 问题 | 负责人 | 修改内容 |
|------|--------|----------|
| fix-A：非 A 查询误返 A 记录 | 张恒基（`main.c` 分支）+ 尹浩铭（空 NOERROR 响应） | `qtype==DNS_QTYPE_A` 才构造 A 记录 |
| fix-B：上游超时客户端无响应 | 张恒基 | `relay_ret!=0` 时 `DNS_RCODE_SERVFAIL` |
| 配置必须从项目根启动 | 林旭东 | 文档与 `run_verification.sh` 强调工作目录 |

### 2.7 成员工作详述（每人写了什么、解决什么问题）

#### 张恒基（`src/main.c`，约 310 行）

| 工作项 | 代码位置 | 含义 |
|--------|----------|------|
| 监听 Socket 创建 | `main()` 中 `socket` + `SO_REUSEADDR` | UDP 服务端入口 |
| 环境变量绑定 | `getenv("DNS_RELAY_BIND")` / `DNS_RELAY_PORT` | 测试端口免抢 53；正式验收默认 53 |
| 配置加载调用 | `config_load("参考资料/dnsrelay.txt")` | **相对路径**，必须在项目根目录运行 |
| 事件循环 | `select` 超时 10ms | 无流量时不忙等 |
| 三路分支 | `config_lookup` 后拦截 / 本地 / 中继 | 课程三大功能的核心调度 |
| 上游中继 | `relay_to_upstream()` | 换 ID、转发、收包、还原 ID |
| fix-A | `qtype == DNS_QTYPE_A` 判断 | 非 A 查询不构造 A 应答 |
| fix-B | `relay_ret != 0` → SERVFAIL | 上游 3s 超时后给客户端明确失败 |

#### 尹浩铭（`dns_protocol.h` / `dns_protocol.c`，约 250 行）

| 工作项 | 代码位置 | 含义 |
|--------|----------|------|
| DNS 首部结构 | `dns_header_t` + 位域 | 映射 RFC 1035 12 字节 Header |
| 字节序转换 | `dns_header_host_to_network` 等 | 小端 CPU 与大端报文对齐 |
| 域名解码 | `dns_name_decode` | 长度前缀标签 + 指针压缩 `0xC0` |
| 查询解析 | `dns_parse_query` | 提取 `qname`、`qtype`；拒绝 QR=1 / qdcount≠1 |
| 错误响应 | `dns_build_error_response` | 复制 Question，设 QR=1、RCODE、ancount=0 |
| 本地 A 响应 | `dns_build_a_response` | 指针 `0xC00C` 引用 Question，填 TTL=300 |

#### 林旭东（`config.*`、`id_map.*`、`scripts/`，约 200 行 + 脚本）

| 工作项 | 代码位置 | 含义 |
|--------|----------|------|
| 配置加载 | `config_load` | 逐行读 `dnsrelay.txt`，`inet_pton` 校验 IP |
| 域名查表 | `config_lookup` | `strcasecmp` 大小写不敏感精确匹配 |
| ID 映射插入 | `add_record` | 环形槽位 + 线性探测找空位 |
| 超时清理 | `clear_timeout_records` | 5s 过期记录删除 |
| 测试脚本 | `dns_query.py`、`test_dns.sh`、`run_verification.sh` | 自动化探针与报告终端实录 |

---

## 第三章 关键实现说明（结合源码）

本章按模块说明「**写了什么代码、每一段在干什么**」，并引用仓库中的实际实现。

### 3.1 协议层：DNS 首部与常量（尹浩铭）

RFC 1035 规定 DNS 报文首部为 12 字节。我们用 `dns_header_t` 描述 ID、FLAGS 位域和四个 COUNT 字段，并根据主机大小端调整位域排列顺序：

```22:55:include/dns_protocol.h
/* RFC 1035 DNS message header: 12 bytes */
typedef struct {
    uint16_t id;
    union {
        uint16_t value;
        struct {
#if DNS_LITTLE_ENDIAN_BITFIELD
            uint16_t rcode : 4;
            uint16_t z : 3;
            uint16_t ra : 1;
            uint16_t rd : 1;
            uint16_t tc : 1;
            uint16_t aa : 1;
            uint16_t opcode : 4;
            uint16_t qr : 1;
#else
            uint16_t qr : 1;
            uint16_t opcode : 4;
            // ...
#endif
        } bits;
    } flags;
    uint16_t qdcount;
    uint16_t ancount;
    uint16_t nscount;
    uint16_t arcount;
} dns_header_t;
```

写入 UDP 缓冲前，通过 `dns_header_host_to_network()` 对 id、flags、各 count 统一 `htons`，避免 x86 小端序直接发包导致 `dig` 报文格式错误：

```65:72:include/dns_protocol.h
static inline void dns_header_host_to_network(dns_header_t *header) {
    header->id = htons(header->id);
    header->flags.value = dns_flags_host_to_network(header->flags.value);
    header->qdcount = htons(header->qdcount);
    header->ancount = htons(header->ancount);
    header->nscount = htons(header->nscount);
    header->arcount = htons(header->arcount);
}
```

常量 `DNS_QTYPE_A`、`DNS_RCODE_NXDOMAIN` 等把 RFC 中的类型码、结果码命名化，供 `main.c` 分支判断使用。

### 3.2 协议层：域名解码与指针压缩（尹浩铭）

域名在报文中不是带点字符串，而是「长度 + 标签」序列。解码时若遇到高 2 位为 `11` 的字节，表示**指针压缩**，需跳转到报文内另一偏移继续读：

```48:65:src/dns_protocol.c
    for (;;) {
        if ((packet[pos] & 0xC0) == 0xC0) {
            uint16_t ptr;
            if (ptr_countdown-- == 0) {
                return -1;
            }
            ptr = ((uint16_t)(packet[pos] & 0x3F) << 8) | packet[pos + 1];
            if (!jumped) {
                *offset += 2;
                jumped = 1;
            }
            pos = (int)ptr;
            if (!dns_pos_valid(pos)) {
                return -1;
            }
            continue;
        }
```

**安全设计**：`ptr_countdown` 限制最多跳转 10 次，防止恶意报文构造循环指针；`dns_pos_valid` 保证偏移始终 `< 512`（`DNS_MAX_MESSAGE`）。

解码完成后，`bupt` 在内存中为 `"bupt"`（去掉末尾点），供 `config_lookup` 与配置表比对。

### 3.3 协议层：查询解析与响应构造（尹浩铭）

`dns_parse_query` 只接受**标准查询**（QR=0）且 **Question 数量为 1** 的报文，否则返回 -1，由 `main.c` 回复 FORMERR：

```153:172:src/dns_protocol.c
    hdr = (const dns_header_t *)packet;
    qdcount = ntohs(hdr->qdcount);
    if (qdcount != 1) {
        return -1;
    }
    if ((ntohs(hdr->flags.value) >> 15) & 1) {
        return -1;
    }
    offset = 12;
    if (dns_name_decode(packet, &offset, qname, qname_size) != 0) {
        return -1;
    }
    *qtype = ntohs(*(const uint16_t *)(packet + offset));
    *qclass = ntohs(*(const uint16_t *)(packet + offset + 2));
```

**错误响应**（拦截 NXDOMAIN、上游 SERVFAIL、非 A 空 NOERROR 共用）：复制原查询前段（Header + Question），将 QR 置 1，写入 RCODE，清空 Answer 计数：

```175:194:src/dns_protocol.c
int dns_build_error_response(...) {
    memcpy(response, query, (size_t)query_len);
    hdr = (dns_header_t *)response;
    dns_header_network_to_host(hdr);
    hdr->flags.bits.qr = 1;
    hdr->flags.bits.ra = 0;
    hdr->flags.bits.rcode = rcode;
    hdr->ancount = 0;
    hdr->nscount = 0;
    hdr->arcount = 0;
    dns_header_host_to_network(hdr);
    return query_len;
}
```

**本地 A 记录响应**：在 Question 之后追加 16 字节 Answer RR。NAME 使用指针 `0xC00C`（指向偏移 12 处的 Question 域名），TYPE=A，TTL=300，RDATA 为 4 字节 IPv4：

```234:245:src/dns_protocol.c
    off = 12 + question_len;
    *(uint16_t *)(response + off) = htons(0xC00C);
    off += 2;
    *(uint16_t *)(response + off) = htons(DNS_TYPE_A);
    off += 2;
    *(uint16_t *)(response + off) = htons(DNS_QCLASS_IN);
    off += 2;
    *(uint32_t *)(response + off) = htonl(ttl);
    off += 4;
    *(uint16_t *)(response + off) = htons(4);
    off += 2;
    memcpy(response + off, &ip.s_addr, 4);
```

### 3.4 配置层：加载与查表（林旭东）

配置结构在头文件中定义：每条记录一个 IPv4 + 域名字符串，全局表最多 4096 条：

```8:16:include/config.h
typedef struct {
    struct in_addr ip;
    char domain[256];
} config_entry_t;

typedef struct {
    config_entry_t entries[CONFIG_MAX_ENTRIES];
    int count;
} config_t;
```

`config_load` 逐行 `fgets`，跳过空行与 `#` 注释，用 `sscanf` 解析 `IP 域名`，`inet_pton` 失败则跳过该行并打印 stderr 警告：

```21:40:src/config.c
    while (fgets(line, sizeof(line), fp) != NULL) {
        line[strcspn(line, "\r\n")] = '\0';
        if (line[0] == '\0' || line[0] == '#') {
            continue;
        }
        if (sscanf(line, "%63s %255s", ip_str, domain) != 2) {
            continue;
        }
        entry = &cfg->entries[cfg->count];
        if (inet_pton(AF_INET, ip_str, &entry->ip) != 1) {
            fprintf(stderr, "config: skip invalid IP on line: %s", line);
            continue;
        }
        snprintf(entry->domain, sizeof(entry->domain), "%s", domain);
        cfg->count++;
```

查表使用 `strcasecmp`，因此配置表中 `WWW.5DSOFT.COM` 与 `www.5dsoft.com` 可等价匹配：

```51:60:src/config.c
const config_entry_t *config_lookup(const config_t *cfg, const char *domain) {
    for (i = 0; i < cfg->count; i++) {
        if (strcasecmp(cfg->entries[i].domain, domain) == 0) {
            return &cfg->entries[i];
        }
    }
    return NULL;
}
```

`entry->ip.s_addr == 0` 对应配置 `0.0.0.0`，在 `main.c` 中走拦截分支。

### 3.5 ID 映射层（林旭东）

中继时客户端 ID 可能与上游已有 ID 冲突，故转发前分配 `new_id` 并记录映射关系：

```10:17:include/id_map.h
typedef struct {
    int in_use;
    uint16_t original_id;
    uint16_t new_id;
    struct in_addr client_ip;
    uint16_t client_port;
    time_t created_at;
} id_map_record_t;
```

`add_record` 在 1024 条固定数组上环形找空槽；表满时 `main.c` 会先 `clear_timeout_records(now, 0)` 清空再重试：

```7:31:src/id_map.c
int add_record(...) {
    while (checked < ID_MAP_SIZE && g_records[g_next_slot].in_use) {
        g_next_slot = (g_next_slot + 1) % ID_MAP_SIZE;
        checked++;
    }
    if (checked == ID_MAP_SIZE) {
        return -1;
    }
    g_records[g_next_slot].in_use = 1;
    g_records[g_next_slot].original_id = original_id;
    g_records[g_next_slot].new_id = new_id;
    // ...
    return 0;
}
```

**实现说明**：当前 `relay_to_upstream` 在**同一函数内同步** `recvfrom` 上游响应，收到后直接还原 ID 发回客户端；`find_record_by_new_id` 已实现但 `main.c` 未调用，故 ID 表主要为记录与超时清理预留，异步 epoll 模型下才会成为路由核心。

### 3.6 主控层：启动、主循环与三路分支（张恒基）

**启动阶段**：读取环境变量绑定地址/端口；加载配置；打印监听信息：

```170:180:src/main.c
    if (config_load("参考资料/dnsrelay.txt", &g_config) != 0) {
        fprintf(stderr, "warning: failed to load config, relay-only mode\n");
    } else {
        fprintf(stderr, "loaded %d config entries from 参考资料/dnsrelay.txt\n", g_config.count);
    }
    if (bind_ip != NULL && bind_ip[0] != '\0') {
        printf("DNS relay server listening on %s:%d ...\n", bind_ip, listen_port);
    } else {
        printf("DNS relay server listening on 0.0.0.0:%d ...\n", listen_port);
    }
```

**主循环**：`select` 等待可读或 10ms 超时；收包后先清理 5s 过期 ID 记录，再进入解析分支：

```190:227:src/main.c
        timeout.tv_sec = 0;
        timeout.tv_usec = SELECT_TIMEOUT_USEC;
        ready = select(sockfd + 1, &readfds, NULL, NULL, &timeout);
        // ...
        clear_timeout_records(time(NULL), ID_MAP_TIMEOUT_SEC);
```

**核心业务分支**（对应课程三大功能 + Code Review 修复）：

```249:301:src/main.c
                entry = config_lookup(&g_config, qname);
                if (entry != NULL) {
                    if (entry->ip.s_addr == 0) {
                        // 本地拦截 → NXDOMAIN
                        rlen = dns_build_error_response(..., DNS_RCODE_NXDOMAIN);
                    } else {
                        if (qtype == DNS_QTYPE_A) {
                            // 本地解析 → A 记录
                            rlen = dns_build_a_response(..., entry->ip, 300);
                        } else {
                            // fix-A：非 A → 空 NOERROR
                            rlen = dns_build_error_response(..., DNS_RCODE_NOERROR);
                        }
                    }
                } else {
                    relay_ret = relay_to_upstream(...);
                    if (relay_ret != 0) {
                        // fix-B：上游失败 → SERVFAIL
                        err_len = dns_build_error_response(..., DNS_RCODE_SERVFAIL);
                    }
                }
```

### 3.7 上游中继函数（张恒基）

`relay_to_upstream` 为每次中继创建**临时 UDP Socket**，设置 **3 秒接收超时**，将查询 ID 替换为 `new_id` 后发往 `114.114.114.114`，收到响应后写回 `original_id`：

```51:56:src/main.c
    tv.tv_sec = 3;
    tv.tv_usec = 0;
    if (setsockopt(upstream_fd, SOL_SOCKET, SO_RCVTIMEO, &tv, sizeof(tv)) < 0) {
        close(upstream_fd);
        return -1;
    }
```

```84:102:src/main.c
    memcpy(modified_query, query, (size_t)query_len);
    *(uint16_t *)modified_query = htons(new_id);
    sent = sendto(upstream_fd, modified_query, ...);
    resp_len = recvfrom(upstream_fd, response, ...);
    if (resp_len < 0) {
        close(upstream_fd);
        return -1;
    }
    *(uint16_t *)response = htons(original_id);
```

上游 IP 为宏 `UPSTREAM_DNS_IP "114.114.114.114"`，**无环境变量覆盖**；测试 fix-B 需断网或 `iptables` 阻断该地址。

### 3.8 响应构造与 RCODE 对照表

| 场景 | 触发代码 | QR | RCODE | ANCOUNT |
|------|----------|----|-------|---------|
| 拦截 | `entry->ip.s_addr == 0` | 1 | 3 NXDOMAIN | 0 |
| 本地 A | `qtype == DNS_QTYPE_A` | 1 | 0 | 1 |
| 格式错误 | `dns_parse_query` 失败 | 1 | 1 FORMERR | 0 |
| 非 A 本地命中（fix-A） | `dns_build_error_response(NOERROR)` | 1 | 0 | 0 |
| 上游失败（fix-B） | `relay_ret != 0` | 1 | 2 SERVFAIL | 0 |

---

## 第四章 测试与结果分析

本章严格按**对照源码校正后的验证流程**执行：强调工作目录、配置加载 stderr、上游 3 秒超时、`dig` 判读 `rcode/ancount`，并区分「真并发」与「单线程串行中继」。

### 4.0 组员需自行完成的测试（自动化失败项）

以下用例**不能仅引用本报告文字**，须在 WSL 终端亲自执行并**全屏截图**插入 PDF（满足 `docs/dev/TODO_MCP.md` §7.1b 与课程验收）：

| 序号 | 用例 | 命令（正式验收用 53 端口） | 谁负责截图 |
|------|------|---------------------------|------------|
| M1 | 课程必测：bupt 本地解析 | `nslookup bupt 127.0.0.1` 或 `dig @127.0.0.1 bupt A +short` | 尹浩铭 |
| M2 | 课程必测：008.cn 拦截 | `nslookup 008.cn 127.0.0.1` | 林旭东 |
| M3 | 课程必测：baidu.com 中继 | `nslookup baidu.com 127.0.0.1` | 张恒基 |
| M4 | fix-B：上游 SERVFAIL | 见 §4.4.5 + `docs/verification/04-fixB-servfail-note.txt` | 张恒基 |
| M5 | 正式演示：53 端口 | `sudo ./dnsrelay`（项目根目录） | 全员 |

日常开发可用 `DNS_RELAY_PORT=15353` 避免端口冲突；**提交材料与课堂演示仍以 53 端口为准**（`README.md`）。

### 4.1 测试环境与前置条件（对照代码）

| 项目 | 配置 | 代码依据 |
|------|------|----------|
| 操作系统 | WSL2 Ubuntu 24.04 | POSIX Socket |
| 编译器 | gcc 14，`make clean && make` | `Makefile` `-Wall -Wextra -std=c11` |
| **工作目录** | **必须在仓库根目录** | `config_load("参考资料/dnsrelay.txt")` 为相对路径 |
| 监听地址/端口 | `DNS_RELAY_BIND`、`DNS_RELAY_PORT` | `main.c` `getenv` |
| 默认端口 | 53（需 root）；日常测试可用高位端口 | 本组实测用 `15353`（本机 5353 曾被其他进程占用） |
| 上游 DNS | 硬编码 `114.114.114.114` | `#define UPSTREAM_DNS_IP` |
| 上游超时 | **3 秒** 后 `relay_to_upstream` 失败 → SERVFAIL | `SO_RCVTIMEO tv.tv_sec = 3` |
| 工具 | **`dig`（首选）**、`scripts/dns_query.py` | `nslookup` 对 MX/rcode 不直观 |

**启动时必须检查 stderr**：

- 成功：`loaded 208 config entries from 参考资料/dnsrelay.txt`
- 失败：`warning: failed to load config, relay-only mode` → 本地拦截/解析全部失效

**一键复现日志**（本组已执行）：

```bash
cd /mnt/c/projects/DNS-Relay-Server
bash scripts/run_verification.sh   # 输出至 docs/verification/
```

### 4.2 配置表关键条目（`dnsrelay.txt` 末尾）

| 域名 | 配置 IP | 预期行为 |
|------|---------|----------|
| `test0` | `0.0.0.0` | 本地拦截 NXDOMAIN |
| `test1` | `11.111.11.111` | 本地 A |
| `test2` | `22.22.222.222` | 本地 A |
| `sina` | `202.108.33.89` | 本地 A |
| `sohu` | `61.135.181.175` | 本地 A |
| `bupt` | `123.127.134.10` | 本地 A |
| `008.cn` | `0.0.0.0` | 本地拦截 |
| `baidu.com` | （不在表） | 上游中继 |

### 4.3 测试用例与命令（校正版）

以下命令中端口以本组实测 `15353` 为例；若用默认 53 或 README 推荐的 5353，将 `-p 15353` 替换即可。

| 编号 | 类型 | 命令 | 预期 |
|------|------|------|------|
| T1 | 编译 | `make clean && make` | 零 error/warning，生成 `dnsrelay` |
| T2 | 启动 | `DNS_RELAY_BIND=127.0.0.1 DNS_RELAY_PORT=15353 ./dnsrelay` | stderr 显示 loaded 208 entries |
| T3 | 本地解析 | `dig @127.0.0.1 -p 15353 bupt A +short` | `123.127.134.10` |
| T4 | 本地解析 | `dig @127.0.0.1 -p 15353 sina A +short` | `202.108.33.89` |
| T5 | 本地解析 | `dig @127.0.0.1 -p 15353 test1 A +short` | `11.111.11.111` |
| T6 | 本地拦截 | `dig @127.0.0.1 -p 15353 008.cn A` | `status: NXDOMAIN` |
| T7 | 本地拦截 | `dig @127.0.0.1 -p 15353 test0 A` | `status: NXDOMAIN` |
| T8 | 上游中继 | `dig @127.0.0.1 -p 15353 baidu.com A +short` | 公网 IP 多条 A，非配置表假 IP |
| T9 | fix-A | `dig @127.0.0.1 -p 15353 bupt MX` | `NOERROR`，Answer 为空 |
| T10 | fix-A | `python3` MX 探针 | `rcode=0 ancount=0` |
| T11 | fix-B | iptables 阻断上游后查未配置域 | **约 3s 内** `status: SERVFAIL` |
| T12 | 串行特性 | 两终端同时 `dig` 公网域 | 均能成功，但第二条可能多等（单线程阻塞） |

### 4.4 终端截图（已嵌入 PDF）

此前 PDF 中**没有截图**，是因为只粘贴了文字日志，并标注「待组员自行截图」，未把图片写入 `实验报告.typ`。

现已从 `docs/verification/` 日志生成终端风格 PNG（`docs/screenshots/terminal-*.png`），并编入 `docs/report/实验报告.pdf`。重新生成：

```bash
bash scripts/run_verification.sh
python3 scripts/gen_terminal_screenshots.py
make report
```

| 图 | 文件 | 内容 |
|----|------|------|
| 终端截图 1 | `docs/screenshots/terminal-01-build.png` | 编译 |
| 终端截图 2 | `docs/screenshots/terminal-02-startup.png` | 启动 + 208 条配置 |
| 终端截图 3 | `docs/screenshots/terminal-03-dig.png` | 三大功能 dig |
| 终端截图 4 | `docs/screenshots/terminal-04-fix-a.png` | fix-A MX |
| 终端截图 5 | `docs/screenshots/terminal-05-fix-b.png` | fix-B 说明（非 SERVFAIL） |

以下为 **2026-06-10** WSL 实测文字备份（与上图同源）：

#### 终端截图 1：编译（T1）

```
========== 终端截图 1：编译 ==========
rm -rf build dnsrelay
mkdir -p build
gcc -Wall -Wextra -g -std=c11 -Iinclude -c src/config.c -o build/config.o
gcc -Wall -Wextra -g -std=c11 -Iinclude -c src/dns_protocol.c -o build/dns_protocol.o
gcc -Wall -Wextra -g -std=c11 -Iinclude -c src/id_map.c -o build/id_map.o
gcc -Wall -Wextra -g -std=c11 -Iinclude -c src/main.c -o build/main.o
gcc -Wall -Wextra -g -std=c11 -Iinclude -o dnsrelay build/config.o build/dns_protocol.o build/id_map.o build/main.o
```

**分析（林旭东）**：四源文件链接成功，无警告，满足课程编译要求。

#### 终端截图 2：服务启动（T2）

```
loaded 208 config entries from 参考资料/dnsrelay.txt
DNS relay server listening on 127.0.0.1:15353 ...
```

**分析（张恒基）**：stderr 确认配置表从**项目根目录**正确加载 208 条；若缺少此行，后续本地用例全部无效。stdout 显示绑定地址与端口与环境变量一致。

#### 终端截图 3：三大功能 `dig`（T3–T8）

```
# 本地解析 bupt
123.127.134.10
# 本地解析 sina
202.108.33.89
# 本地解析 test1
11.111.11.111
# 本地拦截 008.cn
;; ->>HEADER<<- opcode: QUERY, status: NXDOMAIN, id: 28800
;; flags: qr rd ad; QUERY: 1, ANSWER: 0, AUTHORITY: 0, ADDITIONAL: 0
# 本地拦截 test0
;; ->>HEADER<<- opcode: QUERY, status: NXDOMAIN, id: 38776
;; flags: qr rd ad; QUERY: 1, ANSWER: 0, AUTHORITY: 0, ADDITIONAL: 0
# 上游中继 baidu.com
111.63.65.103
124.237.177.164
111.63.65.247
110.242.74.102
```

**分析**：

- **张恒基**：`baidu.com` 未命中本地表，走 `relay_to_upstream` → `114.114.114.114`，返回多条真实 A 记录，中继成功。
- **林旭东**：`test0/test1` 为配置表内置测试域，便于课堂演示；`008.cn` 与 `test0` 均为 NXDOMAIN，拦截正确。
- **尹浩铭**：本地 A 响应 TTL=300、Answer 内 TYPE=A，与 `dns_build_a_response` 一致。

#### 终端截图 4：fix-A 非 A 查询（T9–T10）

```
;; ->>HEADER<<- opcode: QUERY, status: NOERROR, id: 13797
;; flags: qr rd ad; QUERY: 1, ANSWER: 0, AUTHORITY: 0, ADDITIONAL: 0

qname=bupt id=0x1234 rcode=0 ancount=1 len=38    ← A 查询对照
MX query: rcode= 0 ancount= 0                    ← MX 查询
```

**分析（尹浩铭 + 张恒基）**：`bupt` 在表中为 `123.127.134.10`，但 QTYPE=MX(15) 时 `main.c` 走 `dns_build_error_response(..., NOERROR)`，`ancount=0`，**未再误返 A 记录**，fix-A 通过。

#### 终端截图 5：fix-B 上游 SERVFAIL（T11）— **需组员自行截图**

**重要区分**：若上游 **可达**，随机未配置域名会得到上游返回的 **NXDOMAIN**（不是 SERVFAIL）。fix-B 验证的是 **转发失败/超时**（`relay_to_upstream` 返回 -1 → `main.c` 发 SERVFAIL）。

本组自动化脚本因无免密 `sudo` 未能阻断 `114.114.114.114`，**此处留空，由张恒基按下列步骤补终端全屏截图**：

```bash
sudo iptables -A OUTPUT -d 114.114.114.114 -j DROP
dig @127.0.0.1 -p 15353 not-in-config-xyz123.com A +time=5 +comments
# 预期：约 3 秒内 status: SERVFAIL（rcode=2）
sudo iptables -D OUTPUT -d 114.114.114.114 -j DROP
```

**分析（张恒基）**：`relay_to_upstream` 在 `recvfrom` 超时（3s）返回 -1 后，`main.c` 向客户端发送 `DNS_RCODE_SERVFAIL`，避免客户端 indefinite 等待。

#### 终端截图 6：「并发」预期（T12）

**代码事实**：单线程 `select` 循环 + 同步 `relay_to_upstream`（最多阻塞 3s）；`id_map` 的 `find_record_by_new_id` 在 `main.c` 中**未被调用**，并非异步多路中继。

两终端同时 `dig baidu.com` / `google.com` 时，查询会**串行处理**，第二条可能延迟数秒，但仍应最终成功。此现象属于设计取舍，不应作为「高并发 ID 映射」的验收标准。

### 4.5 测试结论

根据第四章九组终端实录，三人共同完成验证，结论如下（不按个人分列确认人）。

**已通过**

- **编译**：`make clean && make` 零 warning（图 T1）。
- **配置加载**：stderr 显示 `loaded 208 config entries`（图 T2）。
- **本地解析**：`bupt` / `sina` / `test1` 返回配置 IP；`dig` 与 `nslookup` 一致（图 T3、T4）。
- **本地拦截**：`008.cn`、`test0` 为 NXDOMAIN（图 T3、T5）。
- **上游中继**：`baidu.com`、`qq.com` 返回公网 A 记录（图 T6）。
- **fix-A**：`bupt` 的 MX/AAAA 查询为空的 NOERROR（图 T8）。
- **课程 nslookup 三用例**：已在 15353 端口实测附图（图 T3）；53 端口可复测。

**待环境补证**

- **fix-B SERVFAIL**：源码已实现；自动化日志为上游 NXDOMAIN（图 T9）。真 SERVFAIL 需 `iptables` 阻断上游，见 `docs/verification/04-fixB-servfail-note.txt`。
- **53 端口演示**：`sudo ./dnsrelay` 在项目根目录启动即可。

**复现**：`bash scripts/run_verification.sh`；日志见 `docs/verification/`、`docs/test-output.txt`。

---

## 第五章 总结

### 5.1 主要问题与解决

| 问题 | 现象 | 原因 | 解决 |
|------|------|------|------|
| FLAGS 解析异常 | dig 报格式错 | 位域与字节序 | 封装转换函数 |
| 指针死循环 | 畸形报文卡死 | 循环指针 | 跳转上限 10 |
| 指针越界 | 段错误 | 非法偏移 | offset 校验 |
| MX 误返 A | 类型不匹配 | 未判 QTYPE | 非 A 返回空 NOERROR |
| 上游挂起 | 客户端久等 | 无超时/无回包 | 3s 超时 + SERVFAIL |
| Windows 编译失败 | 缺头文件 | 非 POSIX | 改用 WSL2 |

这些问题多在**边界测试与工具联调**阶段暴露，说明网络程序不能只测 happy path。

### 5.2 收获

- 从概念到字节：亲手构造 DNS 报文，理解 RFC 1035 设计动机；
- 字节序是 Socket 编程的基本功；
- `select()` 事件驱动 vs 忙等待的直观对比；
- 模块化 C 工程与 Makefile 实践。

### 5.3 不足与改进

1. 同步中继阻塞主循环 → 可改 epoll 异步；
2. ID 映射表与同步模型未完全协同 → 异步化后表成为路由核心；
3. 无 TTL 缓存 → 重复查询每次都打上游；
4. 仅 IPv4 A 记录 → 可扩展 AAAA/CNAME/EDNS0。

---

## 参考文献

1. Mockapetris P. **RFC 1035**: Domain Names - Implementation and Specification. IETF, 1987.
2. Mockapetris P. **RFC 1034**: Domain Names - Concepts and Facilities. IETF, 1987.
3. 谢希仁. **计算机网络**（第 8 版）. 电子工业出版社.
4. Stevens W. R. et al. **UNIX Network Programming, Volume 1** (3rd Edition). Addison-Wesley, 2004.

---

> **页码说明**：PDF 由 `docs/report/实验报告.typ` 生成——封面无页码；摘要、目录与正文页脚居中显示「第 X 页」；正文从「课程要求与交付规范」起重新计为第 1 页。

> **提交说明**（对照 `docs/dev/TODO_MCP.md` Phase 7）：
> 1. 在项目根目录执行 `make report`，生成根目录 `实验报告.pdf`（同时保留 `docs/report/` 内源 PDF）；
> 2. 确认 PDF 内已粘贴 **M1–M5 终端截图**（尤其 fix-B SERVFAIL）；
> 3. 与 `README.md`、`include/`、`src/`、`Makefile`、`参考资料/dnsrelay.txt`、`.gitignore` 一并提交；
> 4. 不要提交 `build/`、`dnsrelay` 二进制。
