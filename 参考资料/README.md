# 参考资料

| 文件 | 说明 |
|------|------|
| **dnsrelay.txt** | 本地 DNS 配置表（208 条），程序默认读取路径 |
| RFC1034/1035、课程 PPT 等 | 本地查阅用，**未纳入 Git**（见根目录 `.gitignore`） |

程序启动时从 `参考资料/dnsrelay.txt` 加载配置，须在**项目根目录**运行 `./dnsrelay`。
